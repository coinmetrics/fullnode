build algorand:
  extends: .generic-build
  variables:
    FULLNODE: algorand
    VERSION: "5_0_0"

publish algorand:
  extends: .generic-publish
  variables:
    FULLNODE: algorand
    VERSION: "5_0_0"
